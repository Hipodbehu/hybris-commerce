$productCatalog = apparelProductCatalog
$catalogVersion = catalogversion(catalog(id[default=$productCatalog]), version[default='Staged'])[unique=true, default=$productCatalog:Staged]

INSERT_UPDATE Question; $catalogVersion; code[unique = true]; question     ; questionCustomer(uid)           ; product($catalogVersion, code)
                      ;                ; Q001               ; What is it?? ; ulf.becker@rustic-hw.com        ; M27160_B ;
                      ;                ; Q002               ; Hi all       ; screwdriverslover@pronto-hw.com ; M27160_B ;